filters = shared_module('vulkan-animation-example', ['vulkan-animation-example.cpp'],
        dependencies: [wayfire],
        install: true,
        install_dir: join_paths(get_option('libdir'), 'wayfire'))